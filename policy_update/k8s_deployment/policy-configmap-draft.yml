apiVersion: v1
kind: ConfigMap
metadata:
  name: opa-policy
  namespace: default
data:
  policy.rego: |
    package istio.authz

    default allow = false

    allow {
      input.method == "POST"
      input.path == "/preprocessing-gateway"
      # Add your custom logic here
    }
